FROM node:20-alpine
WORKDIR /app

# Installe les deps une seule fois (rapide ensuite grâce aux volumes)
COPY package*.json ./
RUN npm ci

# 2) code
COPY . .

# Ports (Nest: API/WS + endpoint logs)
EXPOSE 3000 8081

# Lancement en watch; le code sera monté en volume
CMD ["npm", "run", "start:dev"]
